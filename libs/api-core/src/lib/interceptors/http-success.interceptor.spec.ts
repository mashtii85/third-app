import { HttpSuccessInterceptor } from './http-success.interceptor'

describe('HttpSuccessInterceptor', () => {
  it('should be defined', () => {
    expect(new HttpSuccessInterceptor()).toBeDefined()
  })
})
